#include <stdio.h>

void main()
{
    int x = 90;
    int y = 10;
    printf("the addition is = %i \n ", x + y);
    printf("the multiblication is = %i \n ", x * y);
    printf("the subtraction is = %i  \n ", x - y);
    printf("the divition is = %i \n ", x / y);
}